{
    "signing": {
      "default": {
        "expiry": "{{ CA_EXPIRY }}"
      },
      "profiles": {
        "kubernetes": {
          "usages": ["signing", "key encipherment", "server auth", "client auth"],
          "expiry": "{{ CA_EXPIRY }}"
        }
      }
    }
  }